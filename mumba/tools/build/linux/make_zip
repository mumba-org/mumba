#!/bin/sh

outdir=$1
cfg=mumba/tools/build/linux/FILES.cfg
archive=$outdir/mumba-linux.zip

if [ -z $outdir ]; then
  echo 'usage: make_zip.sh builddir'
  echo
  echo '  builddir   build output directory (e.g., out/Release)'
  exit 1
fi

mumba/tools/build/make_zip.py $outdir $cfg $archive
