#!/bin/sh
# download libc++ and libc++-abi source
# download swift compiler
# install deps
sudo apt install ninja-build gperf clang llvm bison python lld git libx11-dev libatomic1 libxi-dev libxrandr-dev libglib2.0-dev
