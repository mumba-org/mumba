# Copyright 2016 The Chromium Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

import("//build/config/ui.gni")
import("//lib/mojo/public/tools/bindings/mojom.gni")

mojom("input_devices") {
  sources = [
    "input_device_server.mojom",
  ]

  public_deps = [
    "//lib/ui/events/devices/mojo",
  ]

  if (is_chromeos) {
    sources += [ "touch_device_server.mojom" ]
    public_deps += [ "//lib/ui/events/devices/mojo" ]
    if (use_ozone) {
      sources += [ "input_device_controller.mojom" ]
      public_deps += [ "//lib/mojo/public/mojom/base" ]
    }
  }
}
